# Body chart cloud migration

This directory stores database setup used by the booking and electronic chart app.

`body-chart-cloud.sql` creates the authenticated, per-customer/per-visit body chart storage table used by `body-chart.html`.
